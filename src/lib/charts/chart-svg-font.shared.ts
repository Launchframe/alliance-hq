/** Font stack for chart SVG text in browsers (system fonts available). */
export const CHART_SVG_FONT_FAMILY_WEB = "system-ui, sans-serif";

/** Font family name registered via embedded @font-face for server PNG renders. */
export const CHART_SVG_FONT_FAMILY_EMBEDDED = "HQChartFont, sans-serif";
