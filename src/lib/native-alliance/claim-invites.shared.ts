import { ROSTER_MAX_MEMBERS } from "@/lib/members/roster-rank-quota.shared";

/** Max commander claim invites in one bulk request (matches HQ roster cap). */
export const MAX_BULK_CLAIM_INVITES = ROSTER_MAX_MEMBERS;
