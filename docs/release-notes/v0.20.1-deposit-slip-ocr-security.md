---
title: Deposit-slip reliability, roster OCR v1, and security hardening
status: shipped
release_version: 0.20.1
shipped_at: "2026-07-30T23:46:02.403Z"
---

## Working notes

### Banks & deposit slips
- **PR #473 — Dedupe UX:** Deposit-slip review dedupe UX and lifecycle matching.
- **PR #468 / #421 / #435 / #441 / #428 / #471 / #422:** Favorites/mismatch/hero cards; iterative outcome pairing by lifecycle timing; honor officer member clear on commit; preferred-member fail-closed; per-bank commit serialization; dedupe commits by roster member id; restore soft-archived banks on City List re-import.
- **PR #463 — City List review page:** Dedicated import review page.

### Video & OCR
- **PR #476 / #462 / #459 — Roster OCR v1:** Headers, prehistory, denser frames; shield+quota header match; same-line rank header without quota.
- **PR #461 — Reprocess on review:** Reprocess controls on the review page.
- **PR #410 / #415 / #411 / #417 / #464 / #466 / #399 / #406:** Claim extraction shadows without wiping review; keep shared upload when discarding pass sibling; block reprocess during Ashed submit; require `scores:write`; stop downscaling OCR frames; stop last-seen timestamps corrupting power/level; chunk fingerprint shadow; reject known-home mismatch + shadow compare races.

### Trains & Buster Day
- **PR #452 / #438 / #432 / #430 / #439:** Stop history import depleting pools; release depleting pool only after successful replace; Discord `/set-conductor` consumes pool; Top VR unlock gated on active-roster reporters; Simple Mode day-mechanism Change paints selected day.
- **PR #437 / #418:** Buster Day uses snapshot roster powers; unscored null inputs + lock snapshot attaches.

### Security & multi-tenant
- **PR #443 — Discord force-bind:** Close privilege-escalation on force-bind; Ashed replace lock drop fix.
- **PR #433 / #436 / #405 / #457:** Revoke inherited member links on HQ rebind/unlink; bind member-link confirm to alliance + expected UID; gate roster-miss owner queue on server eligibility; localize `/who-is` claim errors.
- **PR #475 — Multi-tenant scope:** Video lists and alliance picker scoped to memberships.
- **PR #429 / #407 / #427:** Redact session cookie ids from job ops APIs; stop returning store `loginToken` in launch JSON; revoke tip-jar codes on HQ unlink.
- **PR #409 / #440:** Member-link exact match and owner lookup fallback gated on server eligibility.

### Other fixes
- **PR #408 — Time-off:** Officer-only cancel auth + named-month year resolve.
- **PR #467 — THP:** Stop geometry zip row shift from noisy labels.
- **PR #420 — Kills:** Older KillScore re-submit no longer reverts newer HQ totals.
- **PR #454 — Settings:** Readable takedown chips inside search field.

## Summary

**Deposit slips**

- **Clearer review** — Dedupe UX, favorites/mismatch hero cards, lifecycle outcome pairing, and a dedicated City List import review page.
- **Safer commits** — Per-bank commit locks, dedupe by roster member, honor officer member clears, and fail-closed preferred-member resolution.

**Video & OCR**

- **Roster OCR v1** — Better rank headers, prehistory frames, denser extraction, and reprocess controls on the review page.
- **Reliability** — Shadows no longer wipe in-progress review; shared uploads survive pass discard; reprocess blocked while Ashed submit is in flight.

**Trains & Buster Day**

- **Pool integrity** — History import and Discord `/set-conductor` no longer leak or double-consume depleting pool slots; replace only releases on success.
- **Buster Day** — Efficiency uses snapshot roster powers; null unscored inputs handled correctly.

**Security & multi-tenant**

- **Tighter bindings** — Discord member-link confirm scoped to alliance + UID; inherited links revoked on rebind; force-bind escalation closed.
- **Scoped access** — Video job lists and alliance picker limited to your memberships; session cookie ids redacted from job APIs; tip-jar tokens revoked on unlink.

## Breaking changes

## Platform maintainer notes

- **Patch release** — Ships as **v0.20.1** (`npm run release:ship -- --yes`; default patch bump).
- **Migration on deploy** (`db:prepare`): `0118_bank_city_list_snapshot_at`. Confirm with `npm run db:validate-journal`.
- **Discord** — Re-register slash commands if guild schemas are stale after deploy.
