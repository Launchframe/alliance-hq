---
title: Banks, battle plan, Price Is Freight, and video OCR reliability
status: shipped
release_version: 0.17.0
shipped_at: "2026-07-11T19:57:45.478Z"
---

## Working notes

### Banks & Season 5
- **PR #246 — Bank management:** Season 5 bank management with investor-risk drop planning, heatmap, and recommended-drop guidance.
- **PR #251 — Deposit Slip History OCR:** Native OCR, review UI, and commit path for bank deposit-slip history videos.

### Battle plan & dashboard
- **PR #231 — Battle plan phase 1:** Scheduling core for alliance battle planning (migrations `0086`–`0089`).
- **PR #238 — Battle plan mobile:** Mobile daily calendar and select anchoring fixes.
- **PR #226 — Analytics hub:** Native alliance analytics dashboard with daily snapshots (`0090`).

### Price Is Freight & trains
- **PR #233 — VS podium leaderboards:** Price Is Freight VS podium on web and Discord.
- **PR #239 — Saturday heavy-hitter lottery:** Saturday draws only from max-ticket overrides with equal chance; reseeds when the pool changes.
- **PR #250 — Discord leaderboard dedupe:** Dedupe PIF Discord posts by alliance; keep max VS score per member when Ashed returns duplicates.

### VR & commanders
- **PR #237 — Commander-scoped VR:** Commander season VR with progress/projection charts (`0091`).
- **PR #242 — Officer VR drilldown:** Officer VR reports drill down from the leaderboard.
- **PR #230 — Power Level + THP:** Consolidate Power Level and THP onto commanders (`0093`).

### Invites & onboarding funnel
- **PR #210 — Welcome invite URLs:** Welcome invite URLs and API share messages.
- **PR #235 — Invite history:** Reveal/copy codes, filters, pagination, deactivate, and fixed share messages (`0094`).

### Video pipeline
- **PR #244 — Storm Team A/B:** Team A/B toggle, HQ persist, and overlap warning (`0092`).
- **PR #240 / #243 / #245 / #247 / #248 / #249 — Review & submit fixes:** Sticky preview under shell header; clearer queue/submit copy; `competition_id` on VSScore submit; HQ alliance ids on jobs for roster dropdowns; Desert Storm auto-provision requires `event_date`; Ashed officers without a processor slot get a hint.
- **PR #241 — Processor slot hint:** Hint Ashed officers who lack a video processor slot.

### Video OCR reliability (serverless)
- **PR #252 / #253 / #257 / #258 — Tesseract on Vercel:** Fix THP Discord OCR worker path (stop Turbopack module id); ship tesseract constants, utils, and Node worker npm deps in NFT; patch Node `getCore` `lstmOnly` bug and prod logger crash.
- **PR #256 — Stuck parsing jobs:** Mark stale extracting/parsing jobs failed so the UI can retry.
- **PR #255 — Slim queue cron:** Phase 2a dispatch-only video queue cron (slim NFT; OCR runs on per-job process route).

### My THP & tests
- **PR #254 — My THP chart:** Pad y-domain so flat mins are not flush with the axis; peak-growth when series regresses; 7-day stale-report banner.
- **PR #207 — OAuth e2e:** Cover OAuth split badge, `ProviderTypeAlreadyLinked`, Google link, and browser OAuth shim.

## Summary

**Banks & Season 5**

- **Bank management** — Plan Season 5 bank drops with investor-risk heatmaps and recommended-drop guidance.
- **Deposit slip OCR** — Upload and review Deposit Slip History videos; OCR commits slips into bank management.

**Battle plan & analytics**

- **Battle plan** — Schedule alliance battle-plan markers on a calendar (phase 1), with mobile daily-calendar fixes.
- **Alliance analytics hub** — Native dashboard with daily alliance snapshots.

**Price Is Freight & VR**

- **VS podium leaderboards** — Price Is Freight standings on web and Discord, with alliance-deduped Discord posts.
- **Saturday heavy-hitter lottery** — Saturday draws only from the max-ticket override pool (equal chance).
- **Commander VR** — Season VR progress and projection charts; officers can drill down from leaderboard reports.
- **Power Level + THP on commanders** — Power Level and Total Hero Power live on the commander profile.

**Invites & video**

- **Invite history & welcome URLs** — Reveal/copy codes, filter and deactivate invites, and share welcome invite URLs with fixed share messages.
- **Storm Team A/B** — Toggle Team A/B on storm videos, persist on HQ, and warn on overlap.
- **Video OCR reliability** — Stuck parsing jobs fail so you can retry; tesseract/worker assets ship correctly on Vercel; the queue cron stays slim and dispatches OCR to per-job routes.
- **My THP chart** — Clearer baseline and a stale-report hint when syncs go quiet.

## Breaking changes

- None for operators. Video queue cron is dispatch-only (Phase 2a); OCR still runs on the per-job process route — no operator action required.

## Platform maintainer notes

- **Minor release** — Ships as **v0.17.0** (`npm run release:ship -- --minor --yes`).
- **Migrations on deploy** (`db:prepare`): `0086`–`0089` battle plan, `0090` alliance daily snapshots, `0091` commander season VR, `0092` video jobs storm team, `0093` commander power stats, `0094` hq invite revoked_at, `0095` bank management. Confirm `drizzle/meta/_journal.json` (`npm run db:validate-journal`).
- **Video OCR / Vercel** — Tesseract Node `getCore` is patched postinstall; NFT must include constants, utils, and worker npm deps (`bmp-js`, etc.). Queue cron no longer embeds OCR — keep `VIDEO_PROCESS` / internal process routes healthy.
- **RBAC** — Bank routes use `bank:write` / alliance-scoped checks; processor-slot hints surface for Ashed officers without a slot.
- **Discord** — PIF leaderboard worker dedupes by alliance; THP Discord screenshot OCR resolves the worker via package path (not Turbopack module id).
