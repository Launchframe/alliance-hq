---
title: Deposit-slip dedupe and timestamp edit stability
status: shipped
release_version: 0.20.2
shipped_at: "2026-07-31T01:31:01.918Z"
---

## Working notes

- **PR #485 — Post-maturity re-deposits:** Peel locked rows timed after the latest matured/looted OCR row out of proximity conflict groups so mature → collect → re-deposit cycles stay separate; keep multi-frame duplicates in one peel group.
- **PR #486 — Timestamp edit stability:** Defer row-sort unfreeze until focus truly leaves the table (datetime-local pickers blur outside tbody); guard blur handlers for non-Node focus targets; Follow Me activates on row click only.

## Summary

- **Deposit-slip dedupe** — Post-maturity re-deposits (mature → collect → re-deposit) no longer get flagged as proximity conflicts when they are legitimate separate events.
- **Review editing** — Timestamp fields no longer jump rows mid-edit when the native datetime picker opens; Follow Me seeks on row click instead of every field focus.

## Breaking changes

## Platform maintainer notes

- **Patch release** — Ships as **v0.20.2** (`npm run release:ship -- --yes`; default patch bump).
- **No new migrations** since v0.20.1.
