---
title: Discord /link fixes — Last War UID lookup and member level sync
status: shipped
release_version: 0.12.0
shipped_at: "2026-06-22T08:42:05.219Z"
---

## Working notes

- Fix Last War player lookup: use `lastwar-platform.lastwargame.com/redemptionCode.php?method=login` (H5 `/api/player/info` path was OSS `NoSuchKey`).
- Harden Discord interaction signature verification (raw body bytes; `DISCORD_BOT_PUBLIC_KEY` alias).
- On successful `/link`, sync `gameUserLevel` from the platform API into `alliance_members.member_level`.

## Summary

- **Discord `/link` works again** — UID lookup uses the live Last War platform API (`redemptionCode.php?method=login`) instead of a dead H5 CDN path.
- **Member level on link** — HQ roster `member_level` updates from `gameUserLevel` when a member links Discord with their UID.
- **Webhook hardening** — Ed25519 verification uses raw request bytes; `DISCORD_BOT_PUBLIC_KEY` accepted as an alias for `DISCORD_PUBLIC_KEY`.

- **Discord `/link` works again** — UID lookup hits the live Last War platform API instead of a dead H5 CDN path.
- **Member level on link** — HQ roster level updates from `gameUserLevel` when a member links Discord with their UID.
- **Webhook hardening** — Ed25519 verification uses raw request bytes; accepts `DISCORD_BOT_PUBLIC_KEY` as an alias for `DISCORD_PUBLIC_KEY`.

## Breaking changes

## Platform maintainer notes

- No migration. Redeploy production after merge; no new env vars required (optional `LASTWAR_PLAYER_LOOKUP_URL` override documented in `.env.example`).
