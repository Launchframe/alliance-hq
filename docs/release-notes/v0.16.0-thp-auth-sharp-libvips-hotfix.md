---
title: My THP tracker, auth self-serve, and sharp libvips hotfix
status: shipped
release_version: 0.16.0
shipped_at: "2026-07-08T22:18:25.244Z"
---

## Working notes

### Hotfix — sharp / libvips (PR #213)
- Global `outputFileTracingIncludes` for sharp + libvips on all Vercel serverless routes.
- Consolidated tracing config in `scripts/vercel/video-ocr-file-tracing.mjs`.
- Lazy-load OCR/sharp imports; extract `resetVideoJobForReprocess` to isolate reprocess routes.

### My Total Hero Power (PR #205, #212)
- `/my-thp` member tracker page and Real Steel follow-ups.

### Auth self-serve (PR #199, #201, #202)
- HQ email change, account merge, OAuth linking by provider ID.

### Video & OCR (PR #165, #177, #166, #167)
- Opening frame capture at 100ms; tesseract bundle + serial OCR fixes shipped earlier in the window.

## Summary

Ships **My Total Hero Power** at `/my-thp`, self-serve auth (email change, account merge, OAuth-by-provider-ID), and a production hotfix for **sharp/libvips** loading on Vercel. Turbopack externalizes `sharp` app-wide; per-route OCR tracing left `libvips-cpp.so` missing on Discord webhooks, member pages, and other non-OCR lambdas — those routes returned 500 until global native tracing landed in #213.

## Breaking changes

- None for operators.

## Platform maintainer notes

- **Minor release** — Ships as **v0.16.0** (`npm run release:ship -- --minor --yes`).
- **Migrations on deploy** (`db:prepare`): none new since v0.15.0 for this slice; confirm journal if deploying from a stale branch.
- **INC — sharp/libvips outage (2026-07-08):**
  - **Impact:** Intermittent 500s on `/api/webhooks/discord/interactions`, `/my-thp`, and other serverless routes — `ERR_DLOPEN_FAILED: libvips-cpp.so.8.18.3: cannot open shared object file`.
  - **Start:** 4:18 PM EST (2026-07-08T21:18:48Z) — earliest `level:error` sharp failure in Vercel logs.
  - **Resolved:** 5:58 PM EST (2026-07-08T22:58:00Z).
  - **TTL:** **1h 39m**.
  - **Root cause:** Turbopack externalizes `sharp` for the whole app; libvips native binaries were only traced on OCR cron routes, not globally.
  - **Fix:** #213 merged 2026-07-08T21:54:43Z — global `"*": sharpNativeFileTracing`.
