---
title: "Hotfix: VR schema migration journal + connect errors"
status: shipped
release_version: 0.6.2
shipped_at: "2026-06-15T22:31:14.801Z"
---

## Working notes

- Register `0004_solid_warpath` and `0012_viral_resistance` in `drizzle/meta/_journal.json` so `db:prepare` applies them on deploy.
- Production was missing `alliances.current_season_key` (and other 0012 tables) after v0.6.0 shipped the code without the journal entry.
- `/api/auth/connect` failed during RBAC alliance upsert with a Drizzle “Failed query” on `alliances` — not an expired Ashed token.
- Return **503** with `schema_out_of_date` when the DB schema is behind, instead of a misleading **401**.

## Summary

Hotfix: VR schema migration journal + connect errors — Register `0004_solid_warpath` and `0012_viral_resistance` in `drizzle/meta/_journal.json` so `db:prepare` applies them on deploy.; Hotfix: missing migration 0012_viral_resistance; Simplify connect onboarding and add optional phone link step.


## Breaking changes

## Platform maintainer notes

- Redeploy applies pending migrations automatically via `db:prepare`.
- To unblock before redeploy: `DATABASE_URL="…" npm run db:migrate` against production.
