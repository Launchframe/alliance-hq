---
title: Beta follow-through — VR sandbox, native trains, video handoff, and invite flexibility
status: shipped
release_version: 0.14.1
shipped_at: "2026-07-02T08:03:14.866Z"
---

## Working notes

- **PR #122 — Beta fast-follow:** OAuth linking from email-only accounts (session link; cold Discord auto-link when verified emails match); VR sandbox mode for practice `/vr` and My VR between seasons; setup guide marks commander-link complete for owners who linked before `ownerHqUserId`; commanders index linked/not-linked filter; native-alliance train VS/event wheels from HQ `member_season_vr` without Ashed; donation rolls show roster-pick guidance instead of Ashed gate; post-upload **process preview** panel before approve/OCR; migration `0071_vr_sandbox`.
- **PR #125 — Video cross-device access:** Fix review 404 when session alliance context mismatched job; expand active queue statuses; officers with enqueue can open queue for handoff; review page shows API errors instead of infinite loading; admin video jobs “All Statuses” filter fix.
- **PR #126 — Invites without game server:** Officer/member invites and join codes no longer require a linked `game_servers` row; server still resolved at member-link time.
- **PR #127 — Enqueue-scoped queue:** Officers with `hq:video:enqueue` can read the queue when session alliance context is unset (jobs they enqueued only); group access delegates to primary pass job during `pending_upload`.
- **PR #128 — My VR i18n:** Full `myVr` namespace (en-US + pt-BR); removes `my-vr-copy.pending.ts`; sandbox notice strings wired to locale files.


## Summary

Beta follow-through — VR sandbox, native trains, video handoff, and invite flexibility — **PR #122 — Beta fast-follow:** OAuth linking from email-only accounts (session link; cold Discord auto-link when verified emails match); VR sandbox mode for practice `/vr` and My VR between seasons; setup guide marks commander-link complete for owners who linked before `ownerHqUserId`; commanders index linked/not-linked filter; native-alliance train VS/event wheels from HQ `member_season_vr` without Ashed; donation rolls show roster-pick guidance instead of Ashed gate; post-upload **process preview** panel before approve/OCR; migration `0071_vr_sandbox`.; **PR #125 — Video cross-device access:** Fix review 404 when session alliance context mismatched job; expand active queue statuses; officers with enqueue can open queue for handoff; review page shows API errors instead of infinite loading; admin video jobs “All Statuses” filter fix.; **PR #126 — Invites without game server:** Officer/member invites and join codes no longer require a linked `game_servers` row; server still resolved at member-link time..

## Breaking changes

- None for operators. Invite creation no longer returns `422 alliance_server_required` for officer/member roles when the alliance lacks a linked game server (intentional product change).

## Platform maintainer notes

- **Patch release** — Ships as **v0.14.1** (default `release:ship` patch bump from v0.14.0).
- **Migrations on deploy** (`db:prepare`): `0071_vr_sandbox` (adds `alliances.vr_sandbox_enabled` / `vr_sandbox_season_key`). Prior beta migrations through `0070_member_season_vr_events` shipped in v0.14.0.
- **VR sandbox disable** — Turning sandbox off deletes practice season VR rows and **alliance-wide** `hq_vr_pending` (in-flight web confirmations). Officers should warn members before disabling mid-practice.
- **Discord** — No slash-command schema change in this slice; `npm run discord:register-commands` only needed if guilds are stale from v0.14.0 UID-only `/link-commander` deploy.
- **Open follow-up** — PR #129 (auth OAuth error-page copy refresh + Playwright coverage for auth/process-preview/VR sandbox) is not on `main` yet; merge before ship if you want that copy and e2e in the same release.
